---
description: Reglas para modelos Eloquent, relaciones y mutators
globs: ["app/Models/**/*.php"]
alwaysApply: true
---

# Reglas – Modelos Eloquent

## Reglas de Modelo
- Definir `$fillable` siempre
- Relaciones explícitas
- Usar Accessors/Mutators modernos (`Attribute::make`)
- Usar Scopes para queries recurrentes

## Relaciones
```php
public function imagenes()
{
    return $this->hasMany(ImagenCabana::class);
}